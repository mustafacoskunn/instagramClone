<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ornek.uygulama.com.instagramklon">
<uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Home.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Like.LikeActivty"></activity>
        <activity android:name=".Profile.ProfileActivtiy"></activity>

        <activity android:name=".Search.SearchActivty"></activity>

        <activity android:name=".Share.ShareActivty"></activity>
        <activity android:name=".Profile.hesapayarlari"></activity>

    </application>

</manifest>